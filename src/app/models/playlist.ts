export class Playlist {
  owner: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  user_r: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  user_rw: string;
  title: string;

  constructor() {
  }
}
